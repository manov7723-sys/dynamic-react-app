server {
    listen 80;
    server_name techneuralai;
    root /usr/share/nginx/html;
    location /app1 { try_files $uri /index.html; }
    location /health { return 200 'ok'; add_header Content-Type text/plain; }
}

server {
    listen 80;
    server_name crewai;
    root /usr/share/nginx/html;
    location /app2 { try_files $uri /index.html; }
    location /health { return 200 'ok'; add_header Content-Type text/plain; }
}

server {
    listen 80;
    server_name lurobenceai;
    root /usr/share/nginx/html;
    location /app3 { try_files $uri /index.html; }
    location /health { return 200 'ok'; add_header Content-Type text/plain; }
}
