server {
    listen 80;
    server_name bookmark.local;
    location / {
        proxy_pass http://localhost/;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name techneuralai.local;
    location / {
        proxy_pass http://localhost/app1;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name crewai.local;
    location / {
        proxy_pass http://localhost/app2;
        proxy_set_header Host $host;
    }
}

server {
    listen 80;
    server_name lurobenceai.local;
    location / {
        proxy_pass http://localhost/app3;
        proxy_set_header Host $host;
    }
}
